package design_pattern.strategy.strategy1.behaviour;

/*
Actually, there is no need to create as many interfaces as the number of behaviours(or algorithms)
We can work with just one interface
Please look at strategy2 package
 */
public interface SpeakBehaviour {
    public void speak();
}
